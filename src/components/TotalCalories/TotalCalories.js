import React from "react";

const totalCalories = props => {
    return (
        <p>
            Total Calories : <strong>{props.totalCalories}</strong>
        </p>
    );
};

export default totalCalories;
